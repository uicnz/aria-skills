interface:
  displayName: "Render Deploy"
  shortDescription: "Deploy applications and managed resources to Render"
  iconSmall: "./assets/render-deploy.svg"
  iconLarge: "./assets/render-deploy.png"
  defaultPrompt: "Use $render-deploy to deploy or configure this application on Render and report the resulting service details."
dependencies:
  tools:
    - type: "mcp"
      value: "render"
      description: "Render MCP server"
      transport: "streamable-http"
      url: "https://mcp.render.com/mcp"
