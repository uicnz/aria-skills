interface:
  displayName: "Netlify Deploy"
  shortDescription: "Deploy and publish web projects through the Netlify CLI"
  iconSmall: "./assets/netlify-deploy.svg"
  iconLarge: "./assets/netlify-deploy.png"
  defaultPrompt: "Use $netlify-deploy to deploy this project and return the preview URL, build settings, and required fixes."
