interface:
  displayName: "Browse"
  shortDescription: "Fast headless browser for QA testing, dogfooding, and evidence capture."
  defaultPrompt: "Use /ship/browse to inspect the target app, interact with it, and capture evidence from the current session."

policy:
  userInvokable: true
  allowImplicitInvocation: false
  allowedTools:
    - Bash
    - Read
    - AskUserQuestion
