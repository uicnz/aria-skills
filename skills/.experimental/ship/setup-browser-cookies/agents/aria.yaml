interface:
  displayName: "Setup Browser Cookies"
  shortDescription: "Import real browser cookies into the headless browse session."
  defaultPrompt: "Use /ship/setup-browser-cookies to bring real browser cookies into the browse session before QA."

policy:
  userInvokable: true
  allowImplicitInvocation: false
  allowedTools:
    - Bash
    - Read
    - AskUserQuestion
