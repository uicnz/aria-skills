# System Skills

This directory is intentionally reserved.

Aria will ship managed `system` skills, but the original placeholder implementations were removed while the Skills primitive, install surface, and on-disk contract are finalized.

Until that work is complete, do not treat `.system/` as a source of canonical runnable skills.
